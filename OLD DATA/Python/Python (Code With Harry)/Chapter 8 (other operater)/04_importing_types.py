from typing import List,Tuple,Dict,Union # we import that so we give the fix value of these

#  tuple have only int value
t : Tuple[int]=(1,2,3,4,)
print(t)

# we do same in all importing type we use str float int etc
