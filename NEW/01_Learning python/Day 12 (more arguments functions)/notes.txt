1. there is many type of argument like default while making fun we add the default value if user dont pass any value so this defult value will run
2. the keyword argument in this if in fun there is first a then b so we want to pass first b then a so we can do this b=9,a=7
3.positional argument in this we only pass value accoding to function